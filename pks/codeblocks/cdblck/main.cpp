#include <stdio.h>

int ReturnPlusOne(int n)
{
    return n + 1;
}

int main(void)
{
    number = 0;
    printf("%d\n",number);

    number = ReturnPlusOne(number);
    printf("%d\n",number);

    return 0;
}
